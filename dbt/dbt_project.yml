name: 'dbtlearn'
version: '0.1'
profile: 'dbtlearn'
config-version: 2

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
    - "target"
    - "dbt_modules"
    - "logs"

models:
  dbtlearn:
    src:
      +materialized: view
    dim:
      +materialized: table
    fct:
      +materialized: table
    mart:
      +materialized: table
